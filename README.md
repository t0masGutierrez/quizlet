I write my notes in Obsidian using markdown and I do active recall with Quizlet. I wanted to use Quizlet's import notes feature but the format of my markdown was incompatible with Quizlet.

The quizlet.py program converts .md into .txt so Quizlet can automatically import my notes.

test
$$
\begin{aligned}
\forall \vec p, \vec q \in \mathbb R^k: \text{d}(\vec p, \vec q) = \sqrt{\sum_{i=1}^k (\vec p_i - \vec q_i)^2}
\end{aligned}
$$
---
